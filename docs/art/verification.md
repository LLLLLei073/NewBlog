# 本地验收记录

2026-09-13，分支 `codex/illustrated-home`，未提交、推送或部署。

- 最终构建生成 19 个页面；站点、房间、助手、插画合计 26 项测试全部通过。格式检查、插画 TypeScript 检查和差异空白检查通过。
- 浏览器检查 1440、375、320 像素宽度；320 × 500 矮屏自然滚动，无横向溢出。正式构建预览位于 http://127.0.0.1:4651/。
- 清晨、正午、黄昏、午夜均检查实际渲染；局部窗光和台灯随时间变化。闭眼局部法线已修正，未见睁眼虹膜残影；眼部像素改变时，脸颊测试像素保持相同。
- 栏目展开、Escape 关闭与焦点返回通过；构建中的内部链接均能解析。房间九个入口保留，小煤球搜索 Codeforces 命中 1 篇，Escape 收起，台灯切换通过。
- 实际首页的测试环境测得：1.1 秒内运行约 30 帧，暂停和后台均 0 帧，恢复约 31 帧；页面恢复、WebGL 上下文丢失及恢复通过。
- 无 WebGL、配套素材失败时保留静态插画和 8 个导航链接；系统减少动态效果时初始绘制 1 帧，播放按钮禁用。
- 临时 public 验收页已删除；可复用验收夹具保留在 tests/fixtures，不进入站点。原始素材、生成提示词和光照近似说明见 README.md。

限制：光照是二维插画上的艺术化模拟，原画阴影和近似法线无法提供真实三维投影。本轮浏览器验收使用桌面浏览器的手机尺寸，没有在实体手机上测量耗电或帧率。构建仍提示现有大体积模块；首页单独加载插画入口，房间模块留在房间页面。
